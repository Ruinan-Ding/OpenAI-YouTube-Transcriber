# requirements.txt

#pip install --upgrade -r OpenAIYouTubeTranscriber/requirements.txt
#or
#python -m pip install --upgrade -r OpenAIYouTubeTranscriber/requirements.txt

requests
py_mini_racer
langdetect
pytubefix
python-dotenv
moviepy
tenacity
git+https://github.com/openai/whisper.git

# Optional: only needed for AI transcript enhancement. The app runs without
# them and prints an install hint if you pick a backend that is missing.
openai
anthropic
transformers
torch